# Inventaire Ansible — Environnement PPRD (pré-production)
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
  children:
    dpscheck_servers:
      hosts:
        localhost:
      vars:
        api_port: 3020
        frontend_port: 4220
        db_port: 5452
        db_password: pprdpwd
        jwt_secret: pprd_secret_change_me